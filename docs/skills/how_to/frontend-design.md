# How-to: /frontend-design

> **Pointer stub** — `/frontend-design` is a **Cursor/Claude plugin**, not a repo SKILL.
> This file exists for discoverability and to keep SKILLS_INDEX.md link-clean.
> There is **no** `SKILL.md` on disk for this skill.

## Metadata

| Field | Value |
|-------|--------|
| **Scope** | GLOBAL |
| **Skill name** | `frontend-design` |
| **SKILL.md path** | **Plugin — install via Cursor/Claude marketplace.** Not a file under `~/.claude\skills\`. |
| **Invoke** | `/frontend-design` |
| **Aggregated guide** | [global_skills.md](../global_skills.md) |

## One-line description

Produce distinctive, production-grade UI designs.

## When to use

- Generating UI components, pages, or full app scaffolds.
- You want code with a clear aesthetic point of view, not generic AI-styled output.
- Breaking out of default Inter/Roboto/Arial + blue-gray-card defaults.

## How to use

```
/frontend-design
```

Then describe what you want built (component, page, or app).

## Output / artifacts

Working HTML/CSS/JS, React, Vue, or whatever framework suits the project. Code is written directly into files; no separate artifact folder.

## Gotchas

- **Installed via marketplace.** Uninstall/reinstall is plugin-side, not a file-system edit under `~/.claude\skills\`.
- If `/frontend-design` doesn't resolve, confirm the plugin is installed in Cursor/Claude — don't go looking for a `SKILL.md`.
- This is **NOT** a hardened project skill; no `/qa-skill-hardening` score applies.
